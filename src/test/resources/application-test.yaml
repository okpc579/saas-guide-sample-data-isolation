spring:
  datasource:
    url: jdbc:h2:mem:data-isolation;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ''
  flyway:
    locations: classpath:db/migration/h2
  jpa:
    hibernate:
      ddl-auto: validate
demo:
  rls:
    enabled: false
